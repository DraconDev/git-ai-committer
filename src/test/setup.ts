import { afterAll, beforeAll, expect, vi } from "vitest";

// Configure global test setup
beforeAll(() => {
    // Mock any global dependencies here
});

// Configure global test teardown
afterAll(() => {
    // Clean up any global state here
});
